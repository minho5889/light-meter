# Review: R3 — App Store compliance pass

## Request
- **Branch:** product/r3-compliance
- **PR:** n/a
- **Implemented by:** Claude (reviewing architect) — **not** the usual Antigravity flow.
  The user directly asked to fast-track R3 to unblock TestFlight (they now hold the
  Sunny InnoLab distribution `.p12`), so the reviewer implemented it. Independent review
  is replaced here by **objective checks**: clean build, clean Release archive, and bundle
  inspection (see below). Antigravity should SKIP R3 and proceed to R4.
- **Summary:** App Store / TestFlight readiness. Added the app icon, privacy manifest,
  export-compliance key, display name, and portrait/full-screen orientation declaration;
  verified no medical claims; added the release runbook.
- **Files touched:**
  - `LightMeter/Assets.xcassets/AppIcon.appiconset/` (1024² opaque icon + Contents.json)
  - `LightMeter/Assets.xcassets/Contents.json`
  - `LightMeter/PrivacyInfo.xcprivacy` (new)
  - `project.yml` (icon name, `ITSAppUsesNonExemptEncryption=false`, `CFBundleDisplayName`,
    `UIRequiresFullScreen`, `UISupportedInterfaceOrientations`)
  - `LightMeter/Info.plist` (regenerated by xcodegen from the above)
  - `docs/release-checklist.md` (new), `README.md`
- **How I verified:**
  - `xcodebuild … clean test` → **161 tests pass, 0 warnings**.
  - `xcodebuild … -configuration Release archive` (CODE_SIGNING_ALLOWED=NO) →
    **ARCHIVE SUCCEEDED, 0 warnings** (the initial iPad-orientation warning was fixed).
  - Bundle inspection of the archived `.app`: `CFBundleIcons.CFBundlePrimaryIcon.
    CFBundleIconName = AppIcon`, `AppIcon60x60@2x.png` emplaced, `PrivacyInfo.xcprivacy`
    bundled, `ITSAppUsesNonExemptEncryption = false`.
  - App icon confirmed **opaque (no alpha)** via `sips` — App Store rejects alpha icons.
- **Acceptance criteria self-check:**
  - Privacy manifest present and correct (no tracking, no collection, UserDefaults reason CA92.1). ✅
  - `NSCameraUsageDescription` reviewed — specific and accurate. ✅
  - Zero banned medical terms in user-facing copy (flicker copy is disclaimer-only). ✅
  - App icon + launch screen present; archive validates clean. ✅
  - Release checklist added under `docs/`. ✅
- **Notes / risks / deviations:**
  - **Distribution signing is NOT wired yet** — still on `CODE_SIGN_STYLE: Automatic`,
    empty team. Needs the `.p12` password + provisioning profile + matching bundle ID
    from Sunny InnoLab. The exact manual-signing steps are in `docs/release-checklist.md`.
    Archive was validated unsigned (`CODE_SIGNING_ALLOWED=NO`).
  - **Localized App Store metadata** (description/keywords per locale) is App Store Connect
    content, owned by Sunny InnoLab at submission — out of scope for the build.
  - App icon is a clean generated placeholder (sun motif); replace with final artwork
    before public release if desired.

## Verdict: APPROVED
- Reviewed by: Claude (reviewing architect)
- Implemented + objectively verified by the reviewer at the user's request to fast-track
  the TestFlight gate. Clean test (161/0) and clean Release archive (0 warnings) with the
  icon, privacy manifest, and compliance keys all confirmed present in the built bundle.
  The remaining work is signing-only and is fully documented for the Sunny InnoLab handoff.

State: APPROVED
