# Review: P3 — Decompose the god object

## Request
- **Branch:** performance/p3-decompose
- **PR:** n/a
- **Summary:** Decomposed the `CameraViewModel` god object by extracting lux, Kelvin, and tint logic into `MeasurementModel`, flicker check logic into `FlickerModel`, and JSON file-based history persistence into `RecordsStore`. `CameraViewModel` remains a thin coordinator managing raw camera session lifecycle. Refactored all child views to consume the new sub-models.
- **Files touched:**
  - `LightMeter/Camera/CameraViewModel.swift`
  - `LightMeter/Camera/MeasurementModel.swift`
  - `LightMeter/Camera/FlickerModel.swift`
  - `LightMeter/Camera/RecordsStore.swift`
  - `LightMeter/Features/Measurement/MeasurementView.swift`
  - `LightMeter/Features/Measurement/CalibrationView.swift`
  - `LightMeter/Features/Temperature/TemperatureView.swift`
  - `LightMeter/Features/Check/FlickerCheckView.swift`
  - `LightMeter/Features/Records/RecordsView.swift`
  - `LightMeterTests/Logic/CameraStateDecompositionTests.swift`
  - `Package.swift`
  - `LightMeter.xcodeproj/project.pbxproj`
  - `README.md`
  - `docs/developer-guide.md`
- **How I verified:**
  - Ran XcodeGen project regeneration: `xcodegen generate`
  - Ran clean build and full test execution:
    ```bash
    export DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer
    xcodebuild -project LightMeter.xcodeproj -scheme LightMeter -destination 'id=59C9F085-A353-40D4-867A-F9296E2FD665' clean build test
    ```
    Result: Build succeeded with zero code warnings. All 153 tests passed.
- **Acceptance criteria self-check:**
  - Each model has a single clear responsibility; no view gains business logic. ✅
  - New/moved logic is tested; all existing tests pass; build clean. ✅
- **Notes / risks / deviations:**
  - `FlickerModel` parameter visibility constraints: Since `CameraSessionActor` is internal to the application target, helper functions on `FlickerModel` that take it as a parameter must not be marked `public`. `FlickerModel` is internal to avoid compiler warnings about parameter visibility.

State: AWAITING_REVIEW

## Verdict: APPROVED
- Reviewed by: Claude (reviewing architect)
- Independently verified: SPM `swift test` 153 pass (confirms the new MeasurementModel/RecordsStore/LightRecord compile in the package target), full-app CLEAN build with ZERO warnings, full-app "All tests passed".
- Clean decomposition: `CameraViewModel` is now a thin coordinator holding three `@Observable @MainActor` sub-models. Frame callbacks route to `measurement.update`/`flicker.update`; behavior is preserved exactly — calibration applied to raw lux, smoother resets on session-start/camera-toggle, flicker description routing (ui_no_light / FlickerInterpreter). `RecordsStore` gains injectable `UserDefaults`. Smart SPM split: FlickerModel excluded (AVFoundation/CameraSessionActor dep), the two pure-ish models included and unit-tested.
- Non-blocking nits: `RecordsStore.loadRecords/saveRecordsToDisk` widened to `public` (minor encapsulation loosening); MeasurementModel/RecordsStore now sit in the SPM "logic" target despite being `@MainActor` glue — pragmatic for testability, acceptable, but it slightly blurs the deterministic-split line. Fine to leave.
- Merged by reviewer per the updated protocol (reviewer owns merges).

State: APPROVED
