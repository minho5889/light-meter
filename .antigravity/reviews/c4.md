# Review: c4 — Reflected-light disclosure & calibration

## Request
- **Branch:** correctness/c4-calibration
- **PR:** n/a
- **Summary:** Added `CalibrationStore` to manage and persist the calibration multiplier. Integrated it into `CameraViewModel` to scale lux calculations, providing actions to calibrate to target lux values and reset back to 1.0. Designed and implemented `CalibrationView` allowing the user to view their current reading, input a known reference target lux value, see a live multiplier preview, and apply or reset the calibration. Integrated a dismissible, glassmorphic reflected-light estimate disclosure on the main LUX tab. Extended localizations to support all disclosure and calibration labels in English, Korean, and French. Updated test counts and added 4 unit tests covering calibration.
- **Files touched:**
  - `LightMeter/Logic/CalibrationStore.swift`
  - `LightMeterTests/Logic/CalibrationStoreTests.swift`
  - `LightMeter/Camera/CameraViewModel.swift`
  - `LightMeter/Features/Measurement/CalibrationView.swift`
  - `LightMeter/Features/Measurement/MeasurementView.swift`
  - `LightMeter/Logic/LocalizedStrings.swift`
  - `LightMeterTests/Logic/LocalizationTests.swift`
  - `Package.swift`
  - `docs/developer-guide.md`
  - `README.md`
- **How I verified:**
  - Verified SPM tests compile and pass via `swift test` (151 tests pass).
  - Verified target builds and test execution via `xcodebuild -project LightMeter.xcodeproj -scheme LightMeter -destination 'id=59C9F085-A353-40D4-867A-F9296E2FD665' test` (all 151 tests pass, build succeeds cleanly).
- **Acceptance criteria self-check:**
  - `CalibrationStore` unit tests (identity by default, correct multiplier calculation, persistence round-trip, sane clamping): ✅ (Tested in `CalibrationStoreTests.swift`)
  - Disclosure visible and dismissible: ✅ (Integrated in `MeasurementView.swift` with dynamic spring transition animations and persistent dismiss state using `@AppStorage`)
  - Calibration reachable from the UI: ✅ (Calibration sheet accessible via a modern "Calibrate" overlay button on the LUX tab)
  - All existing tests pass; build clean: ✅ (151 tests pass, zero compile warnings)
- **Notes / risks / deviations:**
  - None.

State: AWAITING_REVIEW
